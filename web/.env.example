# Base URL of your Strapi API (no trailing slash)
NEXT_PUBLIC_STRAPI_URL=http://localhost:1337

# Optional: a read-only Strapi API token for authenticated access.
# Generate in Strapi admin → Settings → API Tokens.
# STRAPI_API_TOKEN=
